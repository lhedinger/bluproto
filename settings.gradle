// bluproto — multi-module build (MODERNIZATION.md Phase 0).
// engine: the headless simulation (the Swing desktop app is retired; the
//         Java renderer lives on headless as the bake's visual truth).
// server: added in Phase 2 — the web backend that hosts a live world.
rootProject.name = 'bluproto'

include 'engine'
include 'server'
